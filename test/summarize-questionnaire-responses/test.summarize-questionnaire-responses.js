import makeSummarizeQuestionnaireResponses from '../';
import makeSummarizeRequestBuilder from '../../main/core/summarize-questionnaire-responses/models/summarize-request-model.js';

import { strict as assert } from 'assert';

let test_counter = 0;
const begin = Date.now();

// Execute Tests here

const elapsed = Date.now() - begin;
console.log("[TEST] " + test_counter + " tests done in " + elapsed + " ms");